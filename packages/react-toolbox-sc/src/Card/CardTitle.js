import styled, { css } from 'styled-components';
import withOverride from '../utils/withOverride';

const CardTitleNode = styled.div`${withOverride('CardTitleNode')};`;

export default CardTitleNode;
