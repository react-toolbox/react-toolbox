import styled from 'styled-components';

const ListTextSecondary = styled.span`
  color: rgb(117, 117, 117);
  font-size: 14px;
  line-height: 18px;
  white-space: normal;
`;

export default ListTextSecondary;
