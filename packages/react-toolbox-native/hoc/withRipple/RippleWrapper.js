import styled from 'styled-components/native';

const RippleWrapper = styled.View`
  bottom: 0;
  left: 0;
  overflow: hidden;
  position: absolute;
  right: 0;
  top: 0;
`;

export default RippleWrapper;
